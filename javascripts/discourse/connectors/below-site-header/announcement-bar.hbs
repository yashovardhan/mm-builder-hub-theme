{{#if this.enabled}}
  <div class="mm-announcement-bar" role="banner">
    <p class="mm-announcement-bar__text">
      {{this.text}}
      {{#if this.linkUrl}}
        <a href={{this.linkUrl}} target="_blank" rel="noopener noreferrer">{{this.linkLabel}}</a>.
      {{/if}}
    </p>
  </div>
{{/if}}
